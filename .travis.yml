language: go
go:
  - "1.12"

env:
  - GO111MODULE=on

install: true

jobs:
  include:
    - stage: lint
      script:
        - echo ""
        #- make lint
    - stage: run tests
      script:
        - echo ""
        #- IS_CI=true make test
